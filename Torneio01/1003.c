#include <stdio.h>

int main() {
    int valor_a, valor_b, soma;

    scanf("%d", &valor_a);
    scanf("%d", &valor_b);

    soma = valor_a + valor_b;

    printf("SOMA = %d\n", soma);

    return 0;
}