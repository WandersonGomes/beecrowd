#include <stdio.h>

int main() {
    int valor_a, valor_b, soma_ab;

    scanf("%d", &valor_a);
    scanf("%d", &valor_b);

    soma_ab = valor_a + valor_b;

    printf("X = %d\n", soma_ab);

    return 0;
}